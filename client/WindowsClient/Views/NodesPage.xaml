<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WindowsClient.Views.NodesPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="28" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="节点管理" FontSize="22"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <ProgressRing IsActive="{x:Bind Vm.Busy, Mode=OneWay}" Width="20" Height="20"/>
                <Button Content="刷新订阅" Command="{x:Bind Vm.LoadCommand}">
                    <Button.Icon><FontIcon Glyph="&#xE72C;"/></Button.Icon>
                </Button>
                <Button Content="全部测速" Style="{ThemeResource AccentButtonStyle}"
                        Command="{x:Bind Vm.BatchTestCommand}">
                    <Button.Icon><FontIcon Glyph="&#xE9D9;"/></Button.Icon>
                </Button>
            </StackPanel>
        </Grid>
        <TextBlock Grid.Row="1" Text="{x:Bind Vm.Message, Mode=OneWay}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

        <Border Grid.Row="2" Style="{ThemeResource CardBorderFilledStyle}" Padding="4">
            <ListView ItemsSource="{x:Bind Vm.Nodes}"
                      SelectionMode="None" IsItemClickEnabled="False">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:NodeRow"
                                  xmlns:vm="using:WindowsClient.ViewModels">
                        <Grid Padding="14,12" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="3">
                                <TextBlock Text="{x:Bind Name}" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Text="{x:Bind Endpoint}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           FontSize="12" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{x:Bind Latency, Mode=OneWay}"
                                       VerticalAlignment="Center" HorizontalAlignment="Right"
                                       FontWeight="SemiBold"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                <Button Command="{Binding Vm.SelectCommand, ElementName=PageRoot}"
                                        CommandParameter="{x:Bind}" ToolTip="使用此节点"
                                        Style="{ThemeResource AccentButtonStyle}" Padding="16,6">
                                    <TextBlock Text="选择"/>
                                </Button>
                                <Button Command="{Binding Vm.DeleteCommand, ElementName=PageRoot}"
                                        CommandParameter="{x:Bind}" ToolTip="删除"
                                        Padding="8,6">
                                    <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
